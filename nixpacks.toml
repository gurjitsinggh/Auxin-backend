[variables]
NODE_ENV = 'production'
NPM_CONFIG_PRODUCTION = 'false'
NPM_CONFIG_OMIT = 'dev'

[phases.install]
cmds = ['npm ci --omit=dev --no-audit --no-fund']

[phases.build]
cmds = ['npm run build']

[start]
cmd = 'npm run start:prod'