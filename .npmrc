# Use exact versions for better reproducibility
save-exact=true
# Always use package-lock.json for faster installs
package-lock=true
# Disable legacy peer deps warnings
legacy-peer-deps=false
# Explicitly disable production flag to prevent warnings
fund=false
audit=false
